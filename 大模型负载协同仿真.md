# 大模型负载协同仿真

## 概述

本系统实现 **大语言模型（LLM）推理负载** 的端到端协同仿真，整合三个组件：

| 组件 | 角色 | 技术栈 |
|------|------|--------|
| **NeuSight** | 性能预测器 | PyTorch + 机器学习模型 |
| **GPUSim** | 计算仿真器 | Java + CloudSim + JNI |
| **NS-3** | 网络仿真器 | C++ + FNCS 调度器 |
| **FNCS Broker** | 仿真中间件 | C++ + ZeroMQ |

数据流：`NeuSight 预测 → 格式转换 → GPUSim (计算) ↔ FNCS ↔ NS-3 (网络)`

---

## 完整流程

```
                  ┌─────────────────────┐
                  │   NeuSight 预测      │
                  │  (LLM 性能预测 CSV)  │
                  └──────────┬──────────┘
                             │
                             ▼
                  ┌─────────────────────┐
                  │  neusight2cosim.py   │
                  │  CSV → GPUSim + NS-3 │
                  └────┬──────┬─────────┘
                       │      │
              ┌────────▼┐  ┌──▼───────────┐
              │ hosts.json│  │ topology.yaml│
              │ jobs.json │  │ (NS-3 网络拓扑)│
              │(GPUSim任务)│  └──────┬────────┘
              └─────┬────┘         │
                    │              │
                    ▼              ▼
          ┌──────────────────────────────┐
          │        协同仿真               │
          │                               │
          │  ┌──────┐   ┌──────┐   ┌────┐ │
          │  │GPUSim│◄──►│ FNCS │◄──►│NS-3│ │
          │  └──────┘   └──────┘   └────┘ │
          └──────────────────────────────┘
                             │
                             ▼
                  ┌─────────────────────┐
                  │   仿真输出           │
                  │  hostUtils.xml       │
                  │  jobRun.xml          │
                  │  NS-3 网络事件日志   │
                  └─────────────────────┘
```

---

## 文档索引

| 文档 | 路径 | 说明 |
|------|------|------|
| 协同仿真启动指南 | `/home/sdic/协同仿真启动.md` | 容器架构、启动步骤、开发循环 |
| 转换说明 | `/home/sdic/NeuSight/co-simulation-转换说明.md` | NeuSight CSV 到协同仿真文件的转换 |
| 转换脚本 | `/home/sdic/NeuSight/scripts/convert/neusight2cosim.py` | 转换工具源码 |
| 自动化流水线 | `/home/sdic/co_simulation_pipeline.sh` | 一键运行脚本（含超时保护） |

---

## 步骤详解

### 1. NeuSight 性能预测

NeuSight 对 LLM 推理 load 进行性能预测，输出包含每个 CUDA kernel 前向延迟（`fw_latency`）的 CSV 文件：
- 列：`Name, OpName, fw_latency, ...`
- `fw_latency` 以 **毫秒** 为单位
- 包含 transformer 各层（embed, layer_0..N, head）的所有 GPU kernel

### 2. 格式转换（NeuSight → Co-simulation）

转换脚本 `neusight2cosim.py` 完成以下映射：

| NeuSight CSV | GPUSim | NS-3 |
|-------------|--------|------|
| `fw_latency` (ms) | `thread_length` = `fw_latency × 1000` | — |
| kernel Name | GPUSim kernel (block_num=1, thread_num=1) | — |
| — | `hosts.json` (GPU 算力配置，含 8 个必填字段) | — |
| 层执行顺序 | `jobs.json` (层间 children 链路) | — |
| 并行策略 (TP/PP/DP) | 多 GPU 任务分割 | `topology.yaml` (交换机+直连拓扑) |

#### 支持的四类并行策略

| 策略 | 缩写 | 说明 | GPU 间连接 |
|------|------|------|-----------|
| 张量并行 | TP | 每层 tensor 计算拆分到多 GPU | p2p 直连 (800Gbps) |
| 流水线并行 | PP | 各层分配到不同 GPU stage | 通过交换机 |
| 数据并行 | DP | 完整模型复制到多 GPU | 通过交换机 |
| 混合并行 | TP+PP+DP | 组合上述策略 | 混合拓扑 |

### 3. 协同仿真启动

三个 Docker 容器通过 `fncs-net` 桥接网络通信：

```
启动顺序:
  1. FNCS broker      (tcp://fncs:5570)
  2. GPUSim           (等待 broker ACK，阻塞直至所有仿真器就绪)
  3. NS-3             (触发 broker 发送 ACK，仿真开始)
```

> **注意**: FNCS broker 的 ENTRYPOINT 已设为 `fncs_broker`，启动时只需传参数（如 `2` 表示等待 2 个仿真器），不要重复 `fncs_broker` 命令。

#### 关键启动参数

| 容器 | 镜像 | 端口/协议 |
|------|------|-----------|
| fncs | `fncs-broker:built` | tcp://*:5570 (ZeroMQ) |
| gpusim | `gpusim:built` | JNI 调用 libfncs.so |
| ns3 | `ns3:built` | FncsSimulatorImpl |

#### 输入文件

| 文件 | 路径 | 用途 |
|------|------|------|
| `hosts.json` | `/home/sdic/GPUsim/input/hosts.json` | 主机 CPU/GPU/内存配置 |
| `jobs.json` | `/home/sdic/GPUsim/input/jobs.json` | 任务及 GPU kernel 列表 |
| `faults.json` | `/home/sdic/GPUsim/input/faults.json` | 故障注入配置 |
| `topology.yaml` | `/home/sdic/ns3/src/fncs/examples/topology.yaml` | 网络拓扑与仿真停止时间 |
| `fncs.zpl` (gpusim) | `/home/sdic/GPUsim/fncs.zpl` | 订阅 ns3/finish |
| `fncs.zpl` (ns3) | `/home/sdic/ns3/fncs.zpl` | 订阅 gpusim/cloudsim/ * |

#### 数据流

```
GPUSim                          NS-3
  │                               │
  ├──publish("cloudsim/transfer")─►│  ← 传输请求
  ├──publish("cloudsim/end")──────►│  ← 仿真结束信号
  │                               │
  │◄──publish("finish")───────────┤  ← 网络仿真结果
```

### 4. 超时保护机制

协同仿真可能因以下原因无限阻塞：
- FNCS broker 未收到所有仿真器连接
- NS-3 拓扑配置错误导致 FNCS 事件无法处理
- GPUSim 任务配置异常

自动化脚本 `co_simulation_pipeline.sh` 内置 **看门狗定时器**，默认 15 秒后强制终止所有容器并收集已有日志。

---

## 自动化流水线使用

### 一键运行

```bash
./co_simulation_pipeline.sh \
  --csv /home/sdic/NeuSight/scripts/example/out/prediction/.../neusight/gpt3_27-inf-2048-2.csv \
  --tp 2 --pp 2 --dp 1 \
  --num-layers 27
```

### 参数说明

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--csv` | (必填) | NeuSight 预测 CSV |
| `--tp` | 1 | Tensor Parallelism |
| `--pp` | 1 | Pipeline Parallelism |
| `--dp` | 1 | Data Parallelism |
| `--ep` | 1 | Expert Parallelism |
| `--num-layers` | 32 | Transformer 层数 |
| `--sim-stop` | 60 | NS-3 仿真停止时间(秒) |
| `--timeout` | 15 | 协同仿真超时(秒)，超时自动停止 |
| `--skip-convert` | false | 跳过转换，使用已有输入文件 |

### 输出结果

```
./cosim_logs/
├── gpusim_output/         # GPUSim 输出文件（仿真完成时生成）
│   ├── hostUtils.xml      # 主机利用率时序
│   ├── jobRun.xml         # 任务运行结果
│   ├── faultRecords.xml   # 故障记录
│   └── hostFail.xml       # 主机故障时间
├── gpusim.log             # GPUSim 运行日志（可能非常大，含详细仿真步骤）
├── ns3.log                # NS-3 运行日志（FNCS 事件收发记录）
├── fncs.log               # FNCS broker 日志
└── report.txt             # 运行报告
```

### 日志分析关键信息

| 日志内容 | 含义 |
|---------|------|
| `Received N FNCS events` | NS-3 收到 GPUSim 的 FNCS 事件 |
| `Processing FNCS topic 'cloudsim/transfer'` | NS-3 处理网络传输请求 |
| `transfer from 'X' to 'Y'` | GPUSim 层间数据传输 |
| `有N个任务正在执行队列` | GPUSim 当前任务队列状态 |
| `FLOPS: 1.0` | GPU flops_per_core 为 1，thread_length 直接对应时间 |

---

## 测试验证结果

以下是在 GPT-3 27 层推理负载（单卡 tp=1, pp=1, dp=1, 707 kernels, 30s 超时）上的测试结论：

| 检查项 | 状态 | 说明 |
|--------|------|------|
| NeuSight CSV 解析 | ✅ | 42 个有效 kernel 正确解析 |
| 按层分组 | ✅ | embed(3)+layer_0(34)×27+head(2) |
| hosts.json 生成 | ✅ | 含完整 GPUInfo（8 字段）和 CPUInfo（5 字段） |
| jobs.json 生成 | ✅ | 29 个 job，707 个 kernel |
| topology.yaml 生成 | ✅ | sim.stop 正确传递 |
| FNCS broker 启动 | ✅ | 等待 2 个仿真器连接 |
| GPUSim 启动 & 连接 | ✅ | JNI 编译成功，解析输入文件，连接 broker |
| NS-3 启动 & 连接 | ✅ | 收到 FNCS 事件并处理 |
| GPUSim 仿真推进 | ✅ | 30 秒内模拟时间从 0→366,520 |
| 超时看门狗 | ✅ | 30 秒到达后正常停止所有容器 |
| 日志收集 | ✅ | gpusim.log(257MB), ns3.log, report.txt |
| 结果文件 | ✅ | hostUtils.xml, jobRun.xml 等生成 |

### 注意事项

1. **仿真时间较长**: 707 个 kernel 的仿真在 30 秒内未完成，建议真实使用时根据 workload 调整 `--timeout` 参数
2. **GPUSim 日志很大**: 默认在 DEBUG 级别输出全部仿真步骤，可能产生数百 MB 日志
3. **NS-3 FncsApplication name**: GPUSim 发送的 transfer 中源/目的为 job 名（如 `embed`、`layer_0`），NS-3 通过 FncsApplication 的 name 做路由匹配

---

## 开发循环

修改代码后无需重建镜像（代码目录通过 Docker volume 挂载）：

```
宿主机构改源码 → 容器内重新编译 → 重启容器
```

详细开发流程参见 `/home/sdic/协同仿真启动.md#开发与修改代码`。

---

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| GPUSim 卡住无输出 | 等待 broker ACK | 启动 NS-3 即可解除阻塞 |
| WARNING: fncs not initialized | 订阅 topic 不匹配 | 确保 ZPL 包含发布方前缀 `gpusim/` |
| NS-3 找不到 libfncs.so | LD_LIBRARY_PATH 缺失 | 确保挂载 `/fncs` 并设置路径 |
| 容器启动后立即退出 | --rm 导致日志丢失 | 改为不加 --rm 或查看 docker logs |
| 仿真超时 15 秒 | 配置错误或任务量太大 | 查看 `cosim_logs/` 下各组件日志，增大 `--timeout` |
| **java.lang.NullPointerException** | hosts.json 缺少字段 | 确保 `int_mips`, `matrix_mips` 在 cpu_infos 中 |
| FNCS broker 启动报错 | 传入了多余命令 | ENTRYPOINT 已是 `fncs_broker`，只需传参如 `2` |
