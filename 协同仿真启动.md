# 协同仿真启动指南

## 架构概览

三个容器通过 `fncs-net` 网络桥接，由 FNCS broker 协调时间同步：

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   gpusim        │     │   fncs          │     │   ns3           │
│   (计算仿真)     │◄───►│   (仿真中间件)   │◄───►│   (网络仿真)     │
│                 │     │                 │     │                 │
│  port 5570      │     │  tcp://*:5570   │     │  port 5570      │
└─────────────────┘     └─────────────────┘     └─────────────────┘
         │                       │                       │
         └───────────────────────┼───────────────────────┘
                                 │
                          fncs-net (bridge)
```

### 各容器职责

| 容器 | 角色 | 镜像 | 说明 |
|------|------|------|------|
| `fncs` | 仿真中间件 | `fncs-broker:built` | FNCS broker，负责时间同步与消息路由 |
| `gpusim` | 计算仿真器 | `gpusim:built` | GPU 工作流仿真（CloudSim 框架），通过 JNI 调用 FNCS |
| `ns3` | 网络仿真器 | `ns3:built` | NS-3 网络仿真，以 FncsSimulatorImpl 替代默认调度器 |

---

## 前置条件

### 镜像构建

```bash
# 1. 构建 FNCS broker 镜像
docker build -t fncs-broker:built -f /home/sdic/fncs/deploy/Dockerfile /home/sdic/fncs/deploy/

# 2. 构建 GPUSim 镜像
docker build -t gpusim:built -f /home/sdic/fncs/deploy/Dockerfile.gpusim /home/sdic/fncs/deploy/

# 3. 构建 NS-3 镜像
docker build -t ns3:built -f /home/sdic/fncs/deploy/Dockerfile.ns3 /home/sdic/fncs/deploy/
```

### Docker 网络

```bash
# 创建桥接网络（如果不存在）
docker network create fncs-net
```

---

## 启动步骤

### 第 1 步：启动 FNCS broker

```bash
docker run -d --name fncs --network fncs-net --rm fncs-broker:built 2
```

参数 `2` 表示 broker 等待 **2 个仿真器** 连接后才会发送 ACK 授权。

### 第 2 步：启动 GPUSim（计算仿真）

```bash
docker run -d --name gpusim --network fncs-net \
  -v /home/sdic/fncs:/fncs \
  -v /home/sdic/GPUsim:/gpusim \
  gpusim:built
```

启动流程：
1. 容器入口脚本 [entrypoint-gpusim.sh](/home/sdic/fncs/deploy/entrypoint-gpusim.sh) 执行
2. 编译 `libJNIfncs.so`（JNI 包装层，链接 libfncs.so）
3. 设置 `FNCS_BROKER=tcp://fncs:5570` 等环境变量
4. 启动 JVM：`backend.SimEngine`
5. JVM 加载 `libJNIfncs.so`，调用 `fncs_initialize()` 连接 broker
6. 等待 broker ACK（此时会阻塞，因为 NS-3 尚未连接）

### 第 3 步：启动 NS-3（网络仿真）

```bash
docker run -d --name ns3 --network fncs-net \
  --entrypoint bash \
  -v /home/sdic/fncs:/fncs \
  -v /home/sdic/ns3:/ns3 \
  ns3:built -c '
# 建立 macOS 路径兼容层
mkdir -p /Users/davidt/FNCS_install/lib /Users/davidt/FNCS_install/include /opt/homebrew/lib
cp /fncs/src/fncs.hpp /fncs/src/fncs.h /Users/davidt/FNCS_install/include/
cp /fncs/.libs/libfncs.so /Users/davidt/FNCS_install/lib/libfncs.dylib
cp /usr/lib/x86_64-linux-gnu/libzmq.so.5 /opt/homebrew/lib/libzmq.dylib
cp /usr/lib/x86_64-linux-gnu/libczmq.so.4 /opt/homebrew/lib/libczmq.dylib
ln -sf /fncs/.libs/libfncs.so.1 /Users/davidt/FNCS_install/lib/libfncs.so.1
ln -sf /usr/lib/x86_64-linux-gnu/libzmq.so.5 /opt/homebrew/lib/libzmq.so.5
ln -sf /usr/lib/x86_64-linux-gnu/libczmq.so.4 /opt/homebrew/lib/libczmq.so.4

export LD_LIBRARY_PATH=/fncs/.libs:/Users/davidt/FNCS_install/lib:/opt/homebrew/lib:/ns3/build/lib
export FNCS_BROKER=tcp://fncs:5570
export FNCS_NAME=ns3
export FNCS_TIME_DELTA=1ns
export FNCS_FATAL=no

cd /ns3
./build/src/fncs/examples/ns3-dev-fncs-example-default --topo=src/fncs/examples/topology.yaml
'
```

当 NS-3 连接后，broker 检测到 2/2 仿真器已就绪，向双方发送 ACK。两个仿真器同时开始推进仿真时间。

---

## 输入文件

### FNCS 订阅配置（ZPL 格式）

#### GPUSim：`/home/sdic/GPUsim/fncs.zpl`

```zpl
name = gpusim
time_delta = 1ns
broker = tcp://fncs:5570
values
    finish
        topic = ns3/finish
        default = ""
        type = string
        list = false
```

订阅 `ns3/finish` 主题，用于接收 NS-3 网络仿真完成后的回传结果。

#### NS-3：`/home/sdic/ns3/fncs.zpl`

```zpl
name = ns3
time_delta = 1ns
broker = tcp://fncs:5570
values
    cloudsim/transfer
        topic = gpusim/cloudsim/transfer
        default = ""
        type = string
        list = false
    cloudsim/end
        topic = gpusim/cloudsim/end
```

订阅 `gpusim/cloudsim/transfer` 和 `gpusim/cloudsim/end`，用于接收 GPUSim 发出的传输请求和结束信号。

### NS-3 拓扑配置

#### `/home/sdic/ns3/src/fncs/examples/topology.yaml`

```yaml
sim:
  stop: 10s

nodes:
  - id: n0
    type: host
  - id: n1
    type: host
  - id: s0
    type: switch

links:
  - type: csma
    endpoints: [n0, s0]
    dataRate: 100Mbps
    delay: 10us
  - type: csma
    endpoints: [n1, s0]
    dataRate: 100Mbps
    delay: 10us

apps:
  - type: fncs
    node: n0
    name: node1
  - type: fncs
    node: n1
    name: node2
  - type: udpecho-server
    node: n1
    port: 9
  - type: udpecho-client
    node: n0
    remote: n1
    port: 9
    maxPackets: 1
    interval: 1s
    packetSize: 1024
```

### GPUSim 仿真输入

GPUSim 的 `SimEngine.main()` 硬编码了 macOS 路径（从源码读取后覆写参数），容器入口脚本已创建符号链接兼容：

GPUSim 的 `SimEngine.main()` 接受 4 个命令行参数，不再使用硬编码路径：

| 参数位置 | 说明 | 默认值（入口脚本中） |
|----------|------|-------------------|
| `args[0]` | 输出目录 | `/gpusim/OutputFiles` |
| `args[1]` | 主机配置 JSON | `/gpusim/input/hosts.json` |
| `args[2]` | 任务配置 JSON | `/gpusim/input/jobs.json` |
| `args[3]` | 故障注入配置 JSON | `/gpusim/input/faults.json` |
| `args[4]` | 算法（可选，默认 -1=RR） | `-1` |

| 文件 | 说明 |
|------|------|
| `/home/sdic/GPUsim/input/hosts.json` | 主机配置（CPU 核数、MIPS、内存） |
| `/home/sdic/GPUsim/input/jobs.json` | 任务配置（任务长度、GPU 份额、Block 数） |
| `/home/sdic/GPUsim/input/faults.json` | 故障注入配置 |

JSON 文件示例格式：

**hosts.json**:
```json
[
    {
        "name": "host1",
        "video_card_infos": [],
        "cpu_infos": [
            {
                "cores": 2,
                "mips": 10,
                "int_mips": 10,
                "matrix_mips": 10
            }
        ],
        "ram": 4,
        "ifMaster": true
    }
]
```

---

## 数据流与主题路由

```
GPUSim (gpusim)                    NS-3 (ns3)
     │                                  │
     │  publish("cloudsim/transfer")    │
     │  ─────────────────────────────►  │
     │  topic = gpusim/cloudsim/transfer│
     │                                  │
     │  publish("cloudsim/end")         │
     │  ─────────────────────────────►  │
     │  topic = gpusim/cloudsim/end     │
     │                                  │
     │                                  │  publish("finish")
     │  ◄─────────────────────────────  │
     │  topic = ns3/finish              │
```

1. GPUSim 通过 `comm/Api.java` 调用 `JNIfncs.publish("cloudsim/transfer", value)`
2. FNCS 客户端将 topic 加上仿真器名前缀：`gpusim/cloudsim/transfer`
3. Broker 根据 `topic_to_indexes` 路由消息到订阅者
4. NS-3 的 `FncsSimulatorImpl::Run()` 在 `fncs::get_events()` 中收到事件
5. NS-3 解析 `cloudsim/transfer` 事件内容，调用 `FncsApplication::Send()` 注入包到 NS-3 网络仿真
6. 包经过 simulated network（p2p/csma 链路）到达目标节点
7. 目标节点的 `FncsApplication::HandleRead()` 收到包，调用 `fncs::publish("finish", value)` 发回结果
8. GPUSim 通过订阅 `ns3/finish` 接收回传数据

---

## 输出文件

### GPUSim 输出

| 文件 | 说明 |
|------|------|
| `hostUtils.xml` | 各主机的 CPU、RAM、GPU 利用率时序数据 |
| `faultRecords.xml` | 故障注入记录（故障类型、时间、恢复结果） |
| `jobRun.xml` | 任务运行结果（状态、耗时、起止时间、完成度） |
| `hostFail.xml` | 主机故障时间点记录 |
| `gpusim.log` | GPUSim 运行时完整终端日志（通过 `tee` 捕获） |

输出目录由命令行参数指定，入口脚本中默认设为 `/gpusim/OutputFiles`，可通过 `docker exec` 查看或拷贝：

```bash
# 查看输出目录
docker exec gpusim ls /gpusim/OutputFiles/

# 查看运行日志
docker exec gpusim tail -f /gpusim/OutputFiles/gpusim.log

# 拷贝输出到宿主机
docker cp gpusim:/gpusim/OutputFiles /home/sdic/GPUsim/
```

### NS-3 输出

NS-3 的日志输出到容器 stdout，可通过 `docker logs ns3` 查看：

```
Requesting new time from FNCS
Received N FNCS events.
Processing FNCS topic 'cloudsim/transfer'
 transfer from 'src' to 'dst' txt='...' src_task='...'
At time +Xs client received Y bytes from A.B.C.D port Z
```

### FNCS Broker 输出

Broker 在 `LDEBUG4` 级别输出日志，默认不显示。可通过设置 `FNCS_LOG_LEVEL` 环境变量开启详细日志。

---

## 验证命令

```bash
# 查看所有容器状态
docker ps --format '{{.Names}} {{.Status}}'

# 查看 Broker 日志
docker logs fncs

# 查看 GPUSim 日志
docker logs gpusim | tail -20

# 查看 NS-3 日志
docker logs ns3 | tail -20

# 检查 GPUSim 是否还有 FNCS 未初始化警告
docker logs gpusim 2>&1 | grep "WARNING: fncs is not initialized"

# 检查 NS-3 是否收到 FNCS 事件
docker logs ns3 2>&1 | grep "Received.*FNCS events"

# 检查 NS-3 网络仿真是否正常运行
docker logs ns3 2>&1 | grep "client received"
```

---

## 清理

```bash
# 停止所有容器
docker rm -f fncs gpusim ns3
```

---

## 常见问题

### Q: 启动后 GPUSim 无输出 / 卡住

这是正常行为。GPUSim 会在 `fncs_initialize()` 处阻塞，等待 broker 发送 ACK。bBroker 在所有仿真器连接之前不会发送 ACK。启动 NS-3 后，双方会同时解除阻塞。

### Q: 出现 "WARNING: fncs is not initialized"

原因：FNCS 客户端检查 `keys` 集合（其他仿真器订阅了哪些 key），若尝试发布未订阅的 key 会触发此警告。

解决方案：确保订阅方 ZPL 中的 topic 包含发布方的仿真器名前缀。例如，若 GPUSim 发布 `cloudsim/transfer`（实际 topic 为 `gpusim/cloudsim/transfer`），NS-3 的 ZPL 应订阅 `gpusim/cloudsim/transfer`。

### Q: Broker 端口冲突

```bash
# 检查端口占用
docker ps --filter publish=5570

# 停止占用容器
docker stop <container_name>
```

### Q: NS-3 找不到 libfncs.so

确保：
1. `/home/sdic/fncs/.libs/` 存在 `libfncs.so`（需先编译 FNCS）
2. 容器启动时挂载了 `-v /home/sdic/fncs:/fncs`
3. `LD_LIBRARY_PATH` 包含 `/fncs/.libs`

---

## 开发与修改代码

所有三个组件的代码目录均通过 Docker volume 挂载到容器内，因此在**宿主机上修改代码**后，直接在容器内重新编译即可生效，无需重建镜像。

### 通用流程

1. 在宿主机上用任意编辑器修改源码
2. 在对应容器内执行编译命令
3. 重启或重新执行容器入口以运行新代码

---

### GPUSim（Java + JNI）

#### 源码位置

| 内容 | 宿主机路径 | 容器内路径 |
|------|-----------|-----------|
| Java 源码 | `/home/sdic/GPUsim/src/` | `/gpusim/src/` |
| 编译输出 | `/home/sdic/GPUsim/out/` | `/gpusim/out/` |
| JNI C 包装 | `/home/sdic/GPUsim/out/production/gpuworkflowsim/fncs/fncs_JNIfncs.c` | 同上 |
| JAR 依赖 | `/home/sdic/GPUsim/jars/` | `/gpusim/jars/` |

#### 修改 Java 代码后重新编译

```bash
# 在宿主机上修改代码后，在 gpusim 容器内编译单个文件
docker exec gpusim javac \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.jvm=ALL-UNNAMED \
    -d /gpusim/out/production/gpuworkflowsim \
    -cp "/gpusim/out/production/gpuworkflowsim:/gpusim/jars/*" \
    /gpusim/src/backend/SimEngine.java

# 也可以编译整个项目（所有文件）
docker exec gpusim bash -c '
  find /gpusim/src -name "*.java" > /tmp/sources.txt
  javac \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.jvm=ALL-UNNAMED \
    -d /gpusim/out/production/gpuworkflowsim \
    -cp "/gpusim/out/production/gpuworkflowsim:/gpusim/jars/*" \
    @/tmp/sources.txt
'
```

#### 修改 JNI C 包装后重新编译

```bash
# JNI 共享库在容器启动时由 entrypoint 自动编译
# 修改 C 代码或 FNCS 头文件后，手动重新编译：
docker exec gpusim bash -c '
  ln -sf fncs_JNIfncs.h /gpusim/out/production/gpuworkflowsim/fncs/JNIfncs.h
  gcc -shared -fPIC -o /gpusim/lib/libJNIfncs.so \
      -I/usr/lib/jvm/java-21-openjdk-amd64/include \
      -I/usr/lib/jvm/java-21-openjdk-amd64/include/linux \
      -I/fncs/src \
      -I/gpusim/out/production/gpuworkflowsim/fncs \
      /gpusim/out/production/gpuworkflowsim/fncs/fncs_JNIfncs.c \
      -L/fncs/.libs -lfncs
'
```

#### 重启 GPUSim

```bash
docker restart gpusim
# 或停止后重新运行（会重新执行 entrypoint）
docker rm -f gpusim
# 然后重新执行启动步骤中的 docker run 命令
```

---

### FNCS（C++，autotools）

#### 源码位置

| 内容 | 宿主机路径 | 容器内路径 |
|------|-----------|-----------|
| 源码 | `/home/sdic/fncs/src/` | `/fncs/src/` |
| 头文件 | `/home/sdic/fncs/src/fncs.h` | `/fncs/src/fncs.h` |
| 编译输出 | `/home/sdic/fncs/.libs/` | `/fncs/.libs/` |

#### 修改代码后重新编译

```bash
# 在宿主机上修改代码后，在 fncs 容器内重新编译
docker exec fncs bash -c "cd /fncs && ./configure && make -j\$(nproc)"
```

#### 仅重新编译共享库（快速）

```bash
# 如果只修改了 .cpp 文件，可以直接跑 make
docker exec fncs bash -c "cd /fncs && make -j\$(nproc)"
```

#### 重启 Broker

```bash
docker restart fncs
```

**注意**：修改 FNCS 的 C++ 客户端库后（例如 `fncs.cpp`），依赖于 `libfncs.so` 的其他容器（gpusim 的 JNI 包装、ns3 的可执行文件）也需要**重新编译**以链接新的共享库。

---

### NS-3（C++，CMake）

#### 源码位置

| 内容 | 宿主机路径 | 容器内路径 |
|------|-----------|-----------|
| NS-3 源码 | `/home/sdic/ns3/` | `/ns3/` |
| FNCS 模块 | `/home/sdic/ns3/src/fncs/` | `/ns3/src/fncs/` |
| 构建输出 | `/home/sdic/ns3/build/` | `/ns3/build/` |
| 示例程序源码 | `/home/sdic/ns3/src/fncs/examples/` | `/ns3/src/fncs/examples/` |

#### 修改代码后重新编译

```bash
# 在宿主机上修改代码后，在 ns3 容器内重新构建
docker exec ns3 bash -c "
  export LD_LIBRARY_PATH=/fncs/.libs:/Users/davidt/FNCS_install/lib:/opt/homebrew/lib:/ns3/build/lib
  cd /ns3 && ./ns3 build fncs-example
"
```

#### 完整清理重编

```bash
docker exec ns3 bash -c "
  export LD_LIBRARY_PATH=/fncs/.libs:/Users/davidt/FNCS_install/lib:/opt/homebrew/lib:/ns3/build/lib
  cd /ns3 && ./ns3 clean && ./ns3 configure --enable-examples && ./ns3 build fncs-example
"
```

#### 重启 NS-3

```bash
docker restart ns3
```

---

### 完整开发循环示例

从修改 GPUSim 代码到重新运行协同仿真的完整流程：

```bash
# 1. 在宿主机上修改 Java 源码
vim /home/sdic/GPUsim/src/backend/SimEngine.java

# 2. 编译修改后的 Java 文件
docker exec gpusim javac \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED \
    -J--add-opens=jdk.compiler/com.sun.tools.javac.jvm=ALL-UNNAMED \
    -d /gpusim/out/production/gpuworkflowsim \
    -cp "/gpusim/out/production/gpuworkflowsim:/gpusim/jars/*" \
    /gpusim/src/backend/SimEngine.java

# 3. 删除旧容器，重新启动协同仿真
docker rm -f gpusim ns3 fncs

docker network create fncs-net 2>/dev/null

docker run -d --name fncs --network fncs-net --rm fncs-broker:built 2

docker run -d --name gpusim --network fncs-net \
  -v /home/sdic/fncs:/fncs \
  -v /home/sdic/GPUsim:/gpusim \
  gpusim:built

docker run -d --name ns3 --network fncs-net \
  --entrypoint bash \
  -v /home/sdic/fncs:/fncs \
  -v /home/sdic/ns3:/ns3 \
  ns3:built -c '
mkdir -p /Users/davidt/FNCS_install/lib /Users/davidt/FNCS_install/include /opt/homebrew/lib
cp /fncs/src/fncs.hpp /fncs/src/fncs.h /Users/davidt/FNCS_install/include/
cp /fncs/.libs/libfncs.so /Users/davidt/FNCS_install/lib/libfncs.dylib
cp /usr/lib/x86_64-linux-gnu/libzmq.so.5 /opt/homebrew/lib/libzmq.dylib
cp /usr/lib/x86_64-linux-gnu/libczmq.so.4 /opt/homebrew/lib/libczmq.dylib
ln -sf /fncs/.libs/libfncs.so.1 /Users/davidt/FNCS_install/lib/libfncs.so.1
ln -sf /usr/lib/x86_64-linux-gnu/libzmq.so.5 /opt/homebrew/lib/libzmq.so.5
ln -sf /usr/lib/x86_64-linux-gnu/libczmq.so.4 /opt/homebrew/lib/libczmq.so.4
export LD_LIBRARY_PATH=/fncs/.libs:/Users/davidt/FNCS_install/lib:/opt/homebrew/lib:/ns3/build/lib
export FNCS_BROKER=tcp://fncs:5570
export FNCS_NAME=ns3
export FNCS_TIME_DELTA=1ns
export FNCS_FATAL=no
cd /ns3
./build/src/fncs/examples/ns3-dev-fncs-example-default --topo=src/fncs/examples/topology.yaml
'

# 4. 查看运行状态
docker logs fncs
docker logs gpusim | tail -20
docker logs ns3 | tail -20
```

---

## 文件索引

| 路径 | 说明 |
|------|------|
| `/home/sdic/fncs/deploy/Dockerfile` | FNCS broker 镜像构建文件 |
| `/home/sdic/fncs/deploy/Dockerfile.gpusim` | GPUSim 镜像构建文件 |
| `/home/sdic/fncs/deploy/Dockerfile.ns3` | NS-3 镜像构建文件 |
| `/home/sdic/fncs/deploy/entrypoint-gpusim.sh` | GPUSim 容器入口脚本 |
| `/home/sdic/GPUsim/fncs.zpl` | GPUSim FNCS 订阅配置 |
| `/home/sdic/ns3/fncs.zpl` | NS-3 FNCS 订阅配置 |
| `/home/sdic/ns3/src/fncs/examples/topology.yaml` | NS-3 网络拓扑定义 |
| `/home/sdic/GPUsim/src/backend/SimEngine.java` | GPUSim 主入口 |
| `/home/sdic/ns3/src/fncs/model/fncs-simulator-impl.cc` | NS-3 FNCS 调度器实现 |
| `/home/sdic/ns3/src/fncs/examples/fncs-simulator.cc` | NS-3 FNCS 示例主程序 |
| `/home/sdic/GPUsim/input/hosts.json` | GPUSim 主机输入 |
| `/home/sdic/GPUsim/input/jobs.json` | GPUSim 任务输入 |
| `/home/sdic/GPUsim/input/faults.json` | GPUSim 故障输入 |

---

## 性能优化（2026-05-13）

### 问题背景

协同仿真在加载生产级 workloads（5.7M 行 `jobs.json`，约 30 万子任务）时，初始化阶段即卡住，16 分钟未进入实际仿真。

### 瓶颈分析

**根本原因：`GPUWorkflowEngine.doTaskDeliver()` 存在 O(n²) 算法**

1. **算法缺陷**：每次调用 `doTaskDeliver()` 都会重新扫描 `getCloudletList()` 中全部未提交任务，并对每个任务遍历其所有父任务，检查是否在 `getCloudletReceivedList()` 中。复杂度 O(n × m)，其中 n = 剩余任务数，m = 平均父任务数。

2. **日志洪水**：每次父任务依赖检查输出 2 行日志（"X is parent of Y" + "Y cannot be delivered"）。30 万任务 × 2 父任务 = 60 万行/次扫描。`Log` 类逐字节写入 OutputStream，30 万次调用中 I/O 开销极高。

3. **CommEngine 未触发依赖解锁**：非 packet 任务完成后仅加入 `receivedList`，不调用 `doTaskDeliver()`。下游任务需等到某 packet 任务完成才能解锁，可能造成级联阻塞。

### 优化方案

#### 1. 依赖计数器（反向依赖图）— `GPUWorkflowEngine.java`

**核心思想**：将"每次扫描全部任务"改为"仅处理已完成任务的直接子任务"。

**新增数据结构：**
```java
// 记录每个任务未完成的父任务数量（初始值 = getParent().size()）
protected Map<String, Integer> remainingParentCount;

// 反向依赖图：父任务 → 子任务列表
protected Map<String, List<GpuJob>> childrenMap;

// 是否输出详细依赖日志（仅调试用，默认关闭）
protected static boolean verboseDependencyLog = false;
```

**新增方法：**

| 方法 | 说明 | 复杂度 |
|------|------|--------|
| `buildDependencyMaps()` | 在首次 `doTaskDeliver()` 调用时构建 `remainingParentCount` 和 `childrenMap`，仅执行一次 | O(n × m)，仅一次 |
| `deliverReadyChildren(GpuJob)` | 任务完成后，遍历其直接子任务，递减 `remainingParentCount`。当计数归零时提交子任务 | O(k)，k = 直接子任务数 |
| `submitJob(GpuJob)` | 从 `doTaskDeliver()` 中抽取的公共提交逻辑 | O(1) |

**优化后的 doTaskDeliver()：**
- 首次调用时初始化依赖图（仅一次 O(n × m)）
- 每次调用仅检查 `remainingParentCount`，不再遍历父任务列表
- 调试日志默认关闭（`verboseDependencyLog = false`）

#### 2. 修复 CommEngine 依赖解锁与多Packet处理 — `CommEngine.java`

**问题 1**：非 packet 任务完成后不调用 `doTaskDeliver()`，子任务无法解锁。

**问题 2**：每个子任务对应 1 个 packet，任务可能有 N 个 packet（N = 子任务数）。旧代码仅调用 `receivePacket()` 一次，其余 packet 未消费，任务被错误地放入 `waittingJobs` 永久等待。

**修复**：
- 非 packet 任务路径：`getCloudletReceivedList().add(job)` 后增加 `deliverReadyChildren(job)`
- packet 任务路径：将 `doTaskDeliver()` 替换为 `deliverReadyChildren(job)`
- **多 packet 修复**：用 `while (!job.ifReceiveAll()) { job.receivePacket(); }` 循环消费全部 packet，替代单次 `receivePacket()` + `waittingJobs` 分支
- 移除了无效的 `waittingJobs` 等待逻辑（网络回包实际并不发生，NS-3 无法处理当前拓扑中不存在的 FncsApplications）

#### 3. GPUWorkflowFaultEngine 同步优化 — `GPUWorkflowFaultEngine.java`

- 在 `processCloudletReturn()` 中增加 `deliverReadyChildren()` 调用
- 确保非 CommEngine 路径也使用增量式依赖解锁

#### 4. 日志输出控制 — `SimEngine.java`

- 新增 `--logToFile` 参数控制是否写入 `gpusim.log`
- 为 `true` 时使用双路 OutputStream 同时写入 stdout 和文件
- 建议大 workload 时设置 `GPUSIM_LOG_TO_FILE=false`

### 性能对比

| 指标 | 优化前 | 第一轮优化 | 第二轮优化（最终） |
|------|--------|-----------|-----------------|
| 初始化耗时 | 16 分钟+ | ~2s | ~1s |
| 总任务数 | 146,288 | 146,279 | **8**（合并后） |
| 任务完成率 | 0% | 89.6% | **100%** |
| FNCS 时间同步次数 | 0 | ~270,000 | **~20** |
| 总仿真时间 (wall clock) | 无限 | ~32s | **~4s** |
| CloudSim 仿真时间 | N/A | 32,386 | 20 |
| `gpusim.log` 大小 | N/A | 0 (已禁用) | 0 (已禁用) |
| 依赖检测复杂度 | O(n²) | O(n) | O(1)（只有 8 个任务） |

---

## 任务合并算法详解

### DAG 结构发现

通过流式解析 5.7M 行 `jobs.json`，发现 146,288 个任务实际构成 **8 条完全独立的线性管道**（r0~r7）：

```
r0: r0_c5  → r0_c7  → r0_c9  → ... → r0_c36863  (18,200 tasks)
r1: r1_c5  → r1_c7  → r1_c9  → ... → r1_c29283  (14,408 tasks)
r2: r2_c4  → r2_c6  → r2_c8  → ... → r2_c40735  (20,196 tasks)
...
r7: r7_c3  → r7_c5  → r7_c7  → ... → r7_c35xxx  (17,772 tasks)
```

**关键性质**：
- 每条链内是严格的 1:1 线性依赖（每个任务恰好 1 个父任务、1 个子任务）
- 链与链之间**无任何跨链依赖**（0 个孤儿引用、8 个根任务、8 个叶任务）
- 所有 packet 通信均为链内传输（`rX_cM → rX_cN`，同属一条 rX 链）

### 合并算法 (`mergeLinearChains`)

#### 第一步：构建反向依赖图

从 `c2p`（child → parents）构建 `parent2Children`（parent → children）：

```
原始 c2p:          r0_c5 → {}        (根任务)
                    r0_c7 → {r0_c5}   (r0_c5 是 r0_c7 的父)
                    r0_c9 → {r0_c7}
                    ...

反转得到:           r0_c5 → [r0_c7]
                    r0_c7 → [r0_c9]
                    ...
```

#### 第二步：识别根任务，沿链 DFS

找出所有无父任务者（`c2p[name].isEmpty()`）作为各链起点：

```
r0_c5 (根) → r0_c7 → r0_c9 → ... → r0_c36863 (叶)
r1_c5 (根) → r1_c7 → r1_c9 → ... → r1_c29283 (叶)
...
```

DFS 条件：仅当子任务也是 1:1 关系时继续前进（`children.size() == 1 && c2p[child].size() == 1`），确保只在无分支的线性区段合并。

#### 第三步：合并链上所有任务为 1 个聚合任务

对每条链的 **第一个任务**（`first`）执行以下四个融合操作：

**(1) 聚合 GPU 计算量**
```java
totalGpuBlockLen = Σ gt.getBlockLength() * gt.getNumberOfBlocks()  // 遍历链上每个任务的每个子任务的 GpuTask
first.getTasks().get(0).getGpuTask().setBlockLength(totalGpuBlockLen)
first.getTasks().get(0).getGpuTask().setNumberOfBlocks(1)
```
将整条链上所有 GPU 内核的 `blockLength × numberOfBlocks` 累加，写入合并后任务的唯一 GpuTask。
合并后任务只有 **1 个 GPU 内核**，总计算量 = 原链上所有 GPU 内核之和，产生 1 次 GPU 调度事件（而非原 18K 次）。

> **注意**：`cpu_task` 是屎山代码中的假输入属性，为代码运行而存在，无实际语义。合并时**不予考虑**。

**(2) 继承跨链通信 packet**
```java
first.packets.clear()
first.packets.addAll(last.packets)
```
- **链内 packet**（`r0_c33042 → r0_c33044`，src 和 dst 同属 r0）：随合并被吸收，不再需要（merged task 内部已完成所有计算）
- **跨链 packet**（若存在 `r0_xxx → r1_yyy`）：通过 `last.packets` 保留在合并任务上，**通信不受影响**

**(3) 更新依赖关系**
- `first` 的父关系不变（已在 `parseJsonOfJob` 中设置为链首的父任务集合）
- `last` 的子任务依赖更新：在 `c2p` 中将 `last` 替换为 `first`，使跨链依赖正确指向合并后的任务

#### 第四步：替换任务列表

```java
jobs = mergedJobs  // 146,288 → 8
```

### 通信正确性证明

|| 合并前 | 合并后 | 是否变化 |
|---|--------|--------|---------|
| 链内任务 A→B 的 packet | 存在，由 CommEngine 模拟消费 | 不存在（A、B 已合并为同一任务） | 无外部可见变化 |
| 链内 A 完成 → B 被提交 | B 通过 `deliverReadyChildren` 提交 | B 不存在（已合并且 GPU 内核全部在 merged task 中） | 无外部可见变化 |
| 跨链 packet（若存在） | 存在 | 完整保留在 merged task 上 | **无变化** |
| NS-3 收到的 packet | 合并前：每个内部 packet 都发一次（但被 NS-3 skip） | 合并后：无内部 packet，仅跨链 packet 发送 | 对 NS-3 无影响 |
| CommEngine 通信逻辑 | publish→receivePacket→deliver | 不变（合并后任务仍有 packets 字段） | **无变化** |

**结论**：对于当前 8 条独立链的 DAG，跨链通信本来就不存在，合并仅消除了内部的"自循环"通信开销，不会改变任何外部可见的通信行为。如果未来存在跨链依赖的 DAG，跨链 packet 通过步骤 (3) 完整保留。

### 实验结论

| 阶段 | 优化内容 | 任务完成率 | 耗时 |
|------|---------|-----------|------|
| 优化前 | 无 | 0%（16 分钟+卡在初始化） | 无限 |
| 第一轮 | 依赖计数器 + 日志消除 | 89.6%（131K/146K） | ~32s |
| 第二轮 | Keep-alive + 任务合并 | **100%**（8/8 合并任务） | **~4s** |

### 第二轮优化详解

#### 1. DAG 结构验证

通过流式解析 5.7M 行 jobs.json 分析 DAG 结构：
- 146,288 任务，146,280 父子关系，0 孤儿引用
- **8 条独立线性管道**（r0~r7），每条管道约 1.8 万任务
- 每个任务仅依赖前一个任务（1:1 线性），无分支、无跨链依赖
- 结论：DAG 结构完全有效，具备可完成性

#### 2. 任务合并（mergeLinearChains）

**核心思想**：8 条完全独立的线性管道，将每条管道合并为 1 个聚合任务。

- 识别每条链的根任务（无父任务）→ 沿子任务 DFS 遍历 → 收集整条链
- 聚合执行时长为所有任务 cloudletLength 之和
- 继承链首的父依赖和链尾的子依赖
- 146,288 任务 → **8 任务**（减少 99.995%）

#### 3. Keep-Alive 事件机制

**问题**：CloudSim 的 `runClockTick()` 在 `future` 事件队列为空时立即终止（"No more future events"），不检查仿真是否真正完成。

**修复**：
- `GPUWorkflowEngine.startEntity()` 中调度 `WORKFLOW_KEEP_ALIVE` 事件（间隔 1.0 时间单位）
- 每次触发时检查 `ifFinish()`：未完成则重新调度，完成则自然终止
- **同步修复**：`GPUWorkflowFaultEngine.processOtherEvent()` 此前为空实现，阻断了所有自定义事件（包括 `WORKFLOW_CLOUDLET_END_SIM`），现已修复为转发给父类

#### 4. 多 Packet 消费

**问题**：每个子任务对应 1 个 packet，任务可能有多个 packet。旧代码仅调用一次 `receivePacket()`。

**修复**：`while (!job.ifReceiveAll()) { job.receivePacket(); }` 循环消费全部 packet。

### 使用建议

```bash
# 大 workload 禁用日志文件
docker run -e GPUSIM_LOG_TO_FILE=false ... gpusim:built

# 如需要调试依赖链，可修改 verboseDependencyLog = true 后重新编译
```
